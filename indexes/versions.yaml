# GENERATED FILE — DO NOT EDIT
# Generated from indexes/public_registry.yaml


types:
  ads.channel.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada AdsChannelConfig runtime; ThermistorMakeModel -> ThermistorDeviceType). Carries only the per-channel hardware binding; capture/report tuning lives in operational-params (capture.tuning), not the static layout — no Unit/Exponent (redundant; carried by channel identity) or PollPeriodMs/CapturePeriodS/AsyncCapture/AsyncCaptureDelta."

  ads111x.based.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada Ads111xBasedComponentGt runtime)."

  ads111x.based.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (specialized device-type record; per-family successor to the field ads111x.based.cac.gt, keyed by DeviceType)."

  analog.dispatch:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  async.btu.params:
    latest_version: "100"
    versioning_strategy: "literal"
    versions:
      "100":
        summary: >
          - PicoBoardVariant: add
          - MicropythonVersion: add
      "000":
        summary: "Initial Sema version (registered from the shipped gwsproto AsyncBtuParams wire shape, version 000)."

  atn.bid:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "Fix PqPairs sub-type to price.quantity.unitless:001."
      "001":
        summary: "Legacy AtomicTNode bid, pre-ltn rename; PqPairs accept price.quantity.unitless 000 or 001."

  baseurl.failure.alert:
    latest_version: "100"
    versioning_strategy: "literal"
    versions:
      "100":
        summary: "Initial Sema version (registered from the shipped pico wire shape, version 100)."

  bid:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  capture.tuning:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version — the per-channel capture params (CapturePeriodS, AsyncCapture, AsyncCaptureDelta, PollPeriodMs) extracted from the channel-config family into operational-params."

  channel.config:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - Unit: drop (redundant; unit and scaling are carried by channel identity)
          - Exponent: drop (redundant; unit and scaling are carried by channel identity)
      "000":
        summary: "Initial Sema version"

  channel.readings:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: >
          - ChannelId: remove
          - additionalProperties: true -> false
      "001":
        summary: "Baseline runtime version (no prior version tracked)."

  channel.readings.list.item:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  component.attribute.class.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version (flattened from the scada ComponentAttributeClassGt runtime)."

  connectivity.edge.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  cop.curve:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (Intercept, OatCoeff, LwtCoeff, Min, MinOatF) — the COP model migrated out of scada deployment config into operational params."

  dac.output.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (ChannelName, ActorName, DacChannel). The EEPROM power-on fields and their axiom left in place for zero.ten.power.on in operational-params; chip and board facts are scada driver tables."

  data.channel.gt:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: >
          - InPowerMetering: drop (metering is declared by the consuming transactive channel, not here)
          - PowerMeteringConstraint axiom: drop
      "002":
        summary: >
          - Quantity: add
          - TelemetryName: spaceheat.telemetry.name:006 -> 007
          - TelemetryQuantityConsistency axiom: add
      "001":
        summary: "Baseline runtime version (no prior version tracked)."

  derived.channel.gt:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: >
          - OutputUnit: gw1.unit 000 -> 001
          - OutputUnit: required
          - OutputQuantity: gw1.quantity 000 -> 001
          - OutputQuantity: add
          - OutputUnitQuantityConsistency axiom: add
      "001":
        summary: >
          - InputChannelNames[]: add
          - EmissionMethod: add
          - Parameters: add (Optional)
      "000":
        summary: "Initial Sema version"

  device.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial: ComponentId + DeviceType (pascal.case) + optional DisplayName / HwUid / Description."

  dfr.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada DfrComponentGt runtime)."

  dfr.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada DfrConfig runtime). Carries only the per-channel hardware binding; capture/report tuning lives in operational-params (capture.tuning), not the static layout — no Unit/Exponent (redundant; carried by channel identity) or PollPeriodMs/CapturePeriodS/AsyncCapture/AsyncCaptureDelta."

  egauge.register.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada EgaugeRegisterConfig runtime)."

  electric.meter.cac.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version (flattened from the scada ElectricMeterCacGt runtime)."

  electric.meter.channel.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada ElectricMeterChannelConfig runtime). Carries only the per-channel hardware binding; capture/report tuning lives in operational-params (capture.tuning), not the static layout — no Unit/Exponent (redundant; carried by channel identity) or PollPeriodMs/CapturePeriodS/AsyncCapture/AsyncCaptureDelta."

  electric.meter.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version (flattened from the scada ElectricMeterComponentGt runtime). ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case): the device type lived on the referenced cac, not the component."

  electric.meter.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (specialized device-type record; replaces electric.meter.cac.gt category data, keyed by DeviceType)."

  energy.instruction:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  fis.connect.claims:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  fis.instance.authorization.event:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  flo.params.house0:
    latest_version: "007"
    versioning_strategy: "literal"
    versions:
      "007":
        summary: "- FloGitCommit: add"
      "006":
        summary: >
          - StabilityWeight -> StabilityPenaltyWeight: rename
          - StabilityDecay -> StabilityPenaltyDecay: rename
          - StabilityThresholdKwh -> StabilityPenaltyThresholdKwh: rename
          - StabilityHorizonHours -> StabilityPenaltyHorizonHours: rename
          - MaxHpKwhTh: add
          - MaxLoadKwhTh: add
          - RswtPenaltyEnabled: add
          - StabilityPenaltyEnabled: add
          - RswtPenaltyWeight: add
          - RswtPenaltyDecay: add
          - RswtPenaltyExponentRate: add
          - RswtPenaltyDecayMaxHour: add
      "005":
        summary: >
          - StabilityWeight: add
          - StabilityDecay: add
          - StabilityThresholdKwh: add
          - StabilityHorizonHours: add
          - PreviousPlanHpKwhElList: add (optional)
          - PreviousEstimateStorageKwhNow: add (optional)
      "004":
        summary: >
          - FloAlias: remove
          - FloGitCommit: remove
          - ConstantDeltaT: optional -> required
      "003":
        summary: >
          - InitialThermocline -> InitialThermocline1 + InitialThermocline2
          - InitialMiddleTempF: add
      "002":
        summary: >
          - FloAlias: add
          - FloGitCommit: add
          - BufferAvailableKwh: optional -> required
          - HouseAvailableKwh: optional -> required
          - InitialBottomTempF: optional -> required
      "001":
        summary: >
          - CopMin: add
          - CopMinOatF: add
          - HpIsOff: add
          - HpTurnOnMinutes: add
      "000":
        summary: "Earliest wire shape (single shape, seen 2025-01-04 to 2025-01-06), back-filled from S3 evidence. As v001 but without CopMin, CopMinOatF, HpIsOff, HpTurnOnMinutes and the v001-era optional fields (BufferAvailableKwh, HouseAvailableKwh, InitialBottomTempF, DischargingDdDeltaTF)."

  fsm.atomic.report:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - ActionType: remove
          - Action: scalar -> unconstrained object payload
      "000":
        summary: "Initial Sema version"

  fsm.event:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  fsm.full.report:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- AtomicList[]: fsm.atomic.report:000 -> 001"
      "000":
        summary: "Initial Sema version"

  g.node.cmd.ack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  g.node.cmd.nack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  g.node.create.cmd:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- NewNode rebinds to g.node.gt:006; new axiom 1 LocationlessAtCreation (NewNode.PositionPointId SHALL be null — a location identity is registered after creation)"
      "000":
        summary: "Initial Sema version"

  g.node.forest:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "- Nodes rebind to g.node.gt:006; SendTimeMs joins required"
      "001":
        summary: "- SendTimeMs: new optional sender-clock stamp (epoch ms) at forest assembly; the registry stamps wall-clock — it is never simulated (sender-time standard, first adopter)"
      "000":
        summary: "Initial Sema version"

  g.node.forest.request:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  g.node.gt:
    latest_version: "006"
    versioning_strategy: "literal"
    versions:
      "006":
        summary: "- Axiom 2 (PhysicalGNodeLocations) becomes activation-conditioned: PositionPointId is mandatory only when Status is Active — a Pending physical GNode may be locationless"
      "005":
        summary: "adds axiom 6 GNodeAliasHasBody; no field changes"
      "004":
        summary: "Baseline runtime version (no prior version tracked)."

  g.node.instance.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- Run: add"
      "000":
        summary: "Initial Sema version"

  g.node.reparent.cmd:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: "- NewNode rebinds to g.node.gt:006"
      "001":
        summary: "- Proof: new optional opaque authorization artifact for this re-parent (format is machinery of the authority substrate, mirroring g.node.create.cmd)"
      "000":
        summary: "Initial Sema version"

  glitch:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gpio.relay.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. Board-resident per-relay GPIO component: required BoardComponentId (-> scada.board.component.gt), GpioName (-> the board record's NativeGpioOutputs), ConfigList of exactly one relay.control.config; no DeviceType (TypeName is the kind, the board component carries the board identity)."

  gpio.sensor.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. Board-resident per-sensor GPIO component, the input sibling of gpio.relay.component.gt: required BoardComponentId (-> scada.board.component.gt), GpioName (-> the board record's NativeGpioInputs), SenseMode; binary 1/0 sense; no DeviceType (TypeName is the kind, the board component carries the board identity); no SendToDerived (derived routing is computed from DerivedChannel InputChannelNames); no ConfigList (channel binds via DataChannel, capture tuning in operational params). Replaces gw108.gpio.sensor.component.gt."

  gridworks.ack:
    versioning_strategy: "none"
    summary: "Versionless acknowledgement message."

  gridworks.event.comm.mqtt.connect:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version, mirroring the wire Version string"

  gridworks.event.comm.mqtt.disconnect:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version, mirroring the wire Version string"

  gridworks.event.comm.mqtt.fully.subscribed:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version, mirroring the wire Version string"

  gridworks.event.comm.peer.active:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version, mirroring the wire Version string"

  gridworks.event.comm.response.timeout:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version, mirroring the wire Version string"

  gridworks.event.problem:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version"

  gridworks.event.shutdown:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version, mirroring the wire Version string"

  gridworks.event.startup:
    versioning_strategy: "none"
    summary: "Event message reporting that a node has started."

  gridworks.header:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial published version, matching the gwproto Header wire format deployed in the field."

  gridworks.ping:
    versioning_strategy: "none"
    summary: "Versionless message used for liveness/heartbeat checks."

  gw:
    versioning_strategy: "none"
    summary: "Versionless GridWorks message envelope (gwproto Message wire format)."

  gw.adc.waveform:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.channel.gap.stats:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.channel.jump.stats:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.channel.noise.stats:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.command.interface:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.command.transition:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.dispatch.ack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gw.dispatch.nack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gw.experiment.run:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.house0.layout:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "The complete deployable artifact for a House0-fleet home: every collection and Hydronic required, never a partial layout. The word MEANS has-a-siegenthaler-loop (beech, maple); sieg-less homes are the coming gw.house0.no.sieg. Component set is eGauge / ADS111x TSnap / the House0 board record (Krida expanders + GP8403 DACs) with per-relay and per-output components / pico flow / tank + sim-tank / Hubitat + poller / web-server over the DeviceType record model. Axioms cover id uniqueness, the core and command-node Name/ActorClass contracts (sieg-loop and hp-boss unconditional command nodes, dormant when unused), per-zone heat-call channels, primary-flow source agreement, the transactive-power boundary, required flow sensing, the unconditional sieg manifold surface, the system-model energy channels and the models that produce them, the unconditional actuators (plant relays, 0-10V outputs each bound to an i2c.dac.output.component.gt, zone-circuit relay pairs), and the split-system heat-pump parts (hp-odu, hp-idu) with their components. 12 CommandableHeatPump (biconditional: Hydronic.HpCommandNodeName ⇒ hp-odu/hp-idu/hp-ctrl-box with ComponentId, ActorClass HpTwin, handle under hp-boss; every HpTwin node is the declared one); 11's NoActor clause defers to it. 13 PrefixClosedHandles + 14 ActuatorLeaves: the layout's authored handles are the initial command tree, so the tree word's two axioms hold here too (identical wording). 15 ComponentBinding (every component referenced by exactly one node)."

  gw.house0.operational.params:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "ScadaAlias (the applying Scada's identity — stored/emitted instances self-identify; the layout holds the full GNodes) + CaptureTuningList + ActuationAuthority x ServiceMode (authority dominates; ServiceMode inert outside Active) + SeasonalStorageMode + UseSiegLoop (moved in from gw.hydronic: running the loop is operational) + CopCurve + HeatingCurve + HpTurnOnMinutes + HpMaxKwEl + ShortCycleBuffer + LoadOverestimationPercent + OilBoilerBackup + HorizonHours + OnPeakWindows (gw.tou.window with explicit Days). All required — no assumed defaults. Pairs with its layout by family. HpMaxKwEl moved in from scada deployment config; OnPeakWindows added so this word and gw.nolan.operational.params carry identical field sets. ZeroTenPowerOnList added (zero.ten.power.on per 0-10V output: the power-on level moved out of dac.output.config)."

  gw.hydronic:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: >
          - Typed hydronic block promoted out of gw.house0.layout's freeform Hydronic: Zones (gw1.hvac.zone list), optional ZoneCallCircuits (gw1.zone.call.circuit list), TotalStoreTanks, PrimaryFlowSource (gw.house0.primary.flow.source), Strategy. Axioms: Cardinality, CircuitResolution, LearnedNeedsTempChannel. The Siegenthaler-loop flags left this word: plumbed-ness is what a has-sieg layout word means, and use is operational (gw.house0.operational.params UseSiegLoop).
          - HpCommandNodeName (optional spaceheat.name): the node that takes heat-pump commands under hp-boss; absent = nothing commands the heat pump. The layout words' CommandableHeatPump axiom governs it.
          - Strategy dropped: the control family is the layout word's own TypeName (invariant per word, so not a data field; the same rule that retired SiegLoopPlumbed).

  gw.native.gpio.pin:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Name, BcmPin)."

  gw.nolan.layout:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Axioms mirror the names tiers: 1 TransactivePowerChannel, 2 BoardResolution, 3 CoreShNodesExistenceAndActorClass (identical wording across layout words), 4 CommandNodesExistenceAndActorClass (n, five-v-boss, pico-cycler, hp-boss), 5 RequiredActuators (plant relays incl. the per-tank tank1 element relays + circuit relay binding), 6 RequiredHeatpumpEquipment (hp-odu, hp-ctrl-box with components; a Nolan home is a monobloc), 7 ComponentBinding (every component referenced by exactly one node), 8 RequiredSensing (kind-agnostic names incl. HP and resistive-element power channels), 9 SingleStoreTank. Components are the cac-free DeviceType versions; DeviceTypes holds the specialized <family>.device.type.gt records; relay and opto components are board-generic; nodes/channels ride spaceheat.node.gt/303 + data.channel.gt/003. 10 CommandableHeatPump (biconditional: Hydronic.HpCommandNodeName ⇒ hp-odu/hp-idu/hp-ctrl-box with ComponentId, ActorClass HpTwin, handle under hp-boss; every HpTwin node is the declared one); 6's NoActor clause defers to it. 11 PrefixClosedHandles + 12 ActuatorLeaves: the layout's authored handles are the initial command tree, so the tree word's two axioms hold here too (identical wording)."

  gw.nolan.operational.params:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "ScadaAlias (the applying Scada's identity — stored/emitted instances self-identify; the layout holds the full GNodes) + CaptureTuningList + ActuationAuthority x ServiceMode (authority dominates; ServiceMode inert outside Active) + SeasonalStorageMode + CopCurve + HeatingCurve + HpTurnOnMinutes + HpMaxKwEl + ShortCycleBuffer + LoadOverestimationPercent + OilBoilerBackup + HorizonHours + OnPeakWindows (gw.tou.window with explicit Days). All required — no assumed defaults. Pairs with its layout by family. HeldCircuitPositions dropped. The store/optimization knobs are carried so this word and gw.house0.operational.params have identical field sets — Nolan has no thermal store to season, and they come back out as the functional code that still reads them (LeafAlly selection, layout.lite) is moved off them. ZeroTenPowerOnList added (zero.ten.power.on per 0-10V output: the power-on level moved out of dac.output.config)."

  gw.readings:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.tou.window:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version: Start (inclusive) and End (exclusive) hh.mm plus Days (day.of.week list). Axioms: Start strictly before End (no midnight wrap); Days non-empty and duplicate-free."

  gw.weather.channel.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.cmd.ack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.cmd.nack:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.create.cmd:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.forecast:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.forecast.bundle.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.forecast.channel.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.location.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw.weather.observation:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version."

  gw0.required.energy.layered:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version. A parameterless marker: the word names the computation, so a consumer states which model produced a value rather than agreeing on one out of band."

  gw0.usable.energy.layered:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version. A parameterless marker: the word names the computation, so a consumer states which model produced a value rather than agreeing on one out of band."

  gw1.hvac.zone:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- New shared zone type (Name, Critical, KwhPerDegF, optional TempChannelName) replacing the former parallel ZoneList / CriticalZoneList / ZoneKwhPerDegFList in the hydronic block."

  gw1.scada.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Board descriptor (DeviceType, DisplayName, MinPollPeriodMs, TelemetryNameList, NativeGpio, Expanders, Muxes, I2cRelays, SupportsPinReadback, RelayEnergizedLevel, CtAdc, ThermistorAdcs, Dacs; axioms BusMembership / ExpanderMembership / BoardIdentifierUniqueness / MuxConsistency / RelayEnergizedLevelRange). The record describes what is allowed; deployment choices (DIP-selected expander address, chosen wiring) live on the component and layout."

  gw1.simple.sim.layout:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Minimal baseline stub (TypeName + Version only) to seed the layout round-trip loop; built up bit by bit toward the simulated plant's needs."

  gw1.tank.temp.calibration:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - Depth{1,2,3}B: number -> integer (offset in the OutputUnit/FahrenheitX100 scaling
            domain, mirroring linear.one.dimensional.calibration/000)
      "000":
        summary: "Initial Sema version"

  gw1.tank.temp.calibration.map:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- Buffer/Tank now reference gw1.tank.temp.calibration:001 (integer B)"
      "000":
        summary: "Initial Sema version"

  gw1.telemetry.name.quantity.projection:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gw1.unit.quantity.projection:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  gw1.zone.call.circuit:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Circuit record (CircuitPosition, ServesZone, ActuatorKind, Role, CanCool, SetpointSource, Thermostat, WhitewireChannelName, FailsafeRelayNode, OpsRelayNode); FloorLoopsCannotCool and ReadSetpointNeedsCommsStat axioms."

  gw1.zone.thermostat:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Thermostat record (Kind, optional ComponentId); NoComponentOnDumbStat axiom."

  gw108.gpio.sensor.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version (flattened from the scada Gw108GpioSensorComponentGt runtime). ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case): the device type lived on the referenced cac, not the component. ConfigList dropped."

  gw108.vdc.relay.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version (flattened from the scada Gw108GpioRelayComponentGt runtime, TypeName gw108.vdc.relay.component.gt). ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case): the device type lived on the referenced cac, not the component."

  ha1.params:
    latest_version: "006"
    versioning_strategy: "literal"
    versions:
      "006":
        summary: >
          - HpMaxKwEl: add as optional
          - HpMaxKwTh: required -> optional
          - HpTurnOnMinutes: add
      "005":
        summary: >
          - CopIntercept: add as optional
          - CopOatCoeff: add as optional
          - CopLwtCoeff: add as optional
          - CopMin: add as optional
          - CopMinOatF: add as optional
      "004":
        summary: "Baseline runtime version (no prior version tracked)."
      "003":
        summary: "- StratBossDist010: add"
      "002":
        summary: "- LoadOverestimationPercent: add"
      "001":
        summary: "- MaxEwtF: add"
      "000":
        summary: "Earliest wire shape, back-filled from S3 evidence. The nine coefficient/operating fields, before MaxEwtF (v001), LoadOverestimationPercent (v002) and StratBossDist010 (v003)."

  heartbeat.a:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version."

  heating.curve:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (AlphaTimes10, BetaTimes100, GammaEx6, IntermediatePowerKw, IntermediateRswtF, DdPowerKw, DdRswtF, DdDeltaTF, MaxEwtF) — the house thermal model migrated out of scada deployment config into operational params."

  heating.forecast:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  hourly.electricity.dataset:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version: HourList (utc.iso8601.seconds), PriceUsdPerKwhList, UsageKwhList."

  hp.control.box.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (retires the HpModel enum-plus-settings pattern; per-family device-type record keyed by DeviceType)."

  hp.device.type.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (retires the HpModel enum-plus-settings pattern; per-family device-type record keyed by DeviceType; absorbs HpMaxKwEl as MaxKwEl)."

  hubitat.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada HubitatComponentGt runtime)."

  hubitat.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Flattened from the scada HubitatGt runtime; derived URL helpers excluded. MacAddress moved type:string -> $ref formats/mac.address (lowercase-hex constraint; edited in place, not yet web-published)."

  hubitat.poller.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada HubitatPollerComponentGt runtime)."

  hubitat.poller.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada HubitatPollerGt runtime)."

  i2c.bit.address:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (I2cAddress, RegisterIndex, BitIndex)."

  i2c.bus:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Name, BusNumber)."

  i2c.ct.interface.capability:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. CT sibling of i2c.thermistor.interface.capability: Name, I2cBus, I2cAddress, AdcType, AdcReferenceVolts, Channels. Replaces the generic i2c.adc.capability (deleted; staging-only, single referrer)."

  i2c.dac.capability:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (DacName, I2cBus, I2cAddress, optional MuxName/MuxChannel naming the board mux the DAC sits behind, DacType, Channels; MuxPairing axiom); renames i2c.dac.config — the record describes what the board offers, not a chosen configuration."

  i2c.dac.channel.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (DacChannel, PowerOnRawValue, PowerOnVref, PowerOnGain; EepromRanges axiom)."

  i2c.dac.output.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version. Per-output component on the i2c.relay.component.gt pattern: BoardComponentId (-> scada.board.component.gt), DacName (-> the board record's DAC list), ConfigList of exactly one dac.output.config. Replaces the chip-level i2c.dac.writer.component.gt."

  i2c.dac.writer.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (ComponentId, BoardComponentId, DacName, ConfigList of i2c.dac.channel.config, DisplayName, HwUid; DacChannelUniqueness axiom). Context-dependent identity mirrors the thermistor reader: DacName and BoardComponentId resolve against the source layout."

  i2c.expander:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (ExpanderIdx, I2cBus, ExpanderType, I2cAddress, AllowedI2cAddressList; Axiom 1 AddressSpecification: exactly one of the two address fields)."

  i2c.multichannel.dt.relay.component.gt:
    latest_version: "004"
    versioning_strategy: "literal"
    versions:
      "004":
        summary: >
          - AsyncCaptureDelta: ConfigList[0] example now sets it explicitly (the
            relay.actor.config dependency itself is unchanged from 003: :002)
          - ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case).
            Context-dependent: the device type lived on the referenced cac, not the component.
      "003":
        summary: "- Add I2cBus"
      "002":
        summary: "- ConfigList element: relay.actor.config 001 -> 002"
      "001":
        summary: "Earliest wire shape, back-filled from S3 evidence. As v002 but its ConfigList element is relay.actor.config:001 (pre-DeEnergizedState / EnergizedState)."

  i2c.mux:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (MuxName, I2cBus, I2cAddress, MuxType, Channels)."

  i2c.read.bit:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Bus, Address, TriggerId)."

  i2c.read.bytes:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (Bus, I2cAddress, NumBytes, TriggerId)."

  i2c.read.reg:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Bus, Address, NumBytes, TriggerId)."

  i2c.reg.address:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (I2cAddress, RegisterIndex)."

  i2c.relay.capability:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (RelayName, ExpanderIdx, RegisterIndex, BitIndex, SupportedWiringConfigs, Notes). Replaces i2c.relay.config: 'capability' says what is possible, not a chosen configuration; the expander I²C address moves to the board's i2c.expander entry (fixed or DIP-selectable), the chosen address to the component."

  i2c.relay.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. Board-resident per-relay expander component: required BoardComponentId (-> scada.board.component.gt), RelayName (-> the board record's I2cRelays), ConfigList of exactly one relay.control.config; no DeviceType (TypeName is the kind, the board component carries the board identity)."

  i2c.relay.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (RelayName, I2cBus, Address, SupportedWiringConfigs, Notes)."

  i2c.result:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Operation ref moves to i2c.operation/001; optional Bytes list for multi-byte (ReadBytes) results."
      "000":
        summary: "Initial draft (Bus, Operation, Value, Success, Error, UnixTimeMs, TriggerId)."

  i2c.thermistor.channel.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version. Unit: drop (redundant; unit and scaling are carried by channel identity). Exponent: drop (redundant; unit and scaling are carried by channel identity). CapturePeriodS / AsyncCapture / AsyncCaptureDelta / PollPeriodMs: drop (capture/report tuning moved to operational-params capture.tuning). SendToDerived: drop (derived routing is computed from DerivedChannel InputChannelNames)."

  i2c.thermistor.interface.capability:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (Name, I2cBus, I2cAddress, AdcType, SupportedDataRatesSps — the conversion-rate menu the reader's DataRateSps must come from, AdcReferenceVolts, SeriesResistanceKOhms); renames i2c.thermistor.interface.config — the record describes what the board offers, not a chosen configuration."

  i2c.thermistor.reader.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version. ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case) -> dropped (TypeName is the kind; board identity lives on the board component). Bus, AdcAddress, AdcReferenceVolts, SeriesResistanceKOhms dropped (facts on the board's thermistor-interface capability entry); AdcName added, naming that entry in the board record's ThermistorAdcs; DataRateSps added (the per-conversion data rate the reader writes in the ADC config word — a member of the board ADC's SupportedDataRatesSps); BoardComponentId added, anchoring the reader to its scada.board.component.gt. Context-dependent: AdcName and BoardComponentId are derived from the source layout, which the standalone component does not carry."

  i2c.write.bit:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Bus, Address, Value, TriggerId)."

  i2c.write.byte:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (Bus, I2cAddress, Value, TriggerId)."

  i2c.write.reg:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial draft (Bus, Address, NumBytes, Value, TriggerId)."

  keyparam.change.log:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  latest.price:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  layout.lite:
    latest_version: "013"
    versioning_strategy: "literal"
    versions:
      "013":
        summary: >
          - ShNodes: spaceheat.node.gt:301 -> 303
          - DataChannels: data.channel.gt:002 -> 003
          - TankModuleComponents / FlowModuleComponents / I2cRelayComponent: cac-carrying component versions -> cac-free DeviceType versions (pico.tank.module 011->012, sim.pico.tank.module 000->001, pico.flow.module 000->001, i2c.multichannel.dt.relay 003->004). Context-dependent: the embedded components' DeviceType lives on their cac, not the component.
          - SystemMode -> ActuationAuthority x ServiceMode split (Heating -> Active/Heating, Standby -> Standby/Heating, MonitorOnly -> MonitorOnly/Heating; mechanical, not the blocking reason for context).
          - Strategy (free manifold-variant string) -> HardwareLayoutTypeName (left.right.dot): the loaded layout word's TypeName is the family; a format, not an enum of layout words, so this word publishes independently of the layout words' status.
      "012":
        summary: >
          - DerivedChannels[]: derived.channel.gt:000 | 001 -> 001
          - DataChannels[]: data.channel.gt:001 -> 002
          - Ha1Params: ha1.params:004 | 005 | 006 -> ha1.params:006
          - I2cRelayComponent: i2c.multichannel.dt.relay.component.gt:002 -> 003
          - ShNodes[]: spaceheat.node.gt:300 | 301 -> 301
      "011":
        summary: >
          - Ha1Params: ha1.params:004 | 005 -> ha1.params:004 | 005 | 006
          - DerivedChannels[]: derived.channel.gt:000 -> 000 | 001
      "010":
        summary: >
          - ShNodes[]: spaceheat.node.gt:300 -> 300 | 301
          - I2cRelayComponent: required -> optional
      "009":
        summary: >
          - BufferShortCycling: add
          - Ha1Params: 004 -> 004 | 005
      "008":
        summary: >
          - SystemMode: add
          - SeasonalStorageMode: add
          - ShNodes[]: spaceheat.node.gt:200 -> 300
          - TankModuleComponents[]: pico.tank.module.component.gt only -> pico.tank.module.component.gt | sim.pico.tank.module.component.gt
          - DerivedNodeConsistency axiom: add
      "007":
        summary: "Replace SynthChannels with DerivedChannels; add optional TMap."
      "006":
        summary: "Drop FromGNodeInstanceId; add CriticalZoneList."
      "005":
        summary: "Runtime layout carrying FromGNodeInstanceId; no CriticalZoneList (added in 006)."
      "004":
        summary: "Historical back-fill from S3 wire evidence. As v005 but Ha1Params and TankModuleComponents are deploy-lag unions: Ha1Params oneOf[002, 003]; TankModuleComponents.items oneOf[pico.tank 000, 010]."
      "003":
        summary: "Historical back-fill from S3 wire evidence. As v004 but the deploy-lag unions sit one window earlier: Ha1Params oneOf[001, 002]; TankModuleComponents single pico.tank 000 (v010 first appears in v004)."
      "002":
        summary: "Historical back-fill from S3 wire evidence. As v003 but one window earlier: Ha1Params single ha1.params:001 (v002 first appears in v003), and I2cRelayComponent oneOf[i2c 001, 002] (deploy-lag union; i2c/relay.actor.config bumped to 002 on 2024-12-31)."
      "001":
        summary: "Historical back-fill from S3 wire evidence. As v002 but without I2cRelayComponent and SynthChannels, and Ha1Params is the nine-field ha1.params:000 (before MaxEwtF)."

  linear.one.dimensional.calibration:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version. B: number -> integer. Semantics: clarify OutputUnit scaling domain."

  machine.states:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  maker.api.attribute.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada MakerAPIAttributeGt runtime; snake_case fields re-cased to CamelCase)."

  market.product:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  new.command.tree:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: >
          - ShNodes: spaceheat.node.gt:302 -> 303
          - Axiom 2 ActuatorLeaves: FiveVBoss joins the command-node classes
      "002":
        summary: >
          - ShNodes: spaceheat.node.gt:301 -> 302
          - Axiom 2 ActuatorLeaves: every actuator (Relay / ZeroTenOutputer / HpTwin) is a dotted-handle leaf; every dotted-handle leaf is an actuator or a command node (LocalControl / LeafAlly / PicoCycler / HpBoss / SiegLoop, or a NoActor child of the LocalControl node). gw1.actor.class:013 joins the axiom dependencies.
      "001":
        summary: "- ShNodes: spaceheat.node.gt:301 only (drop the multi-version oneOf; retroactive registration of the wire version the spruce scada emitted from 2026-01-28)"
      "000":
        summary: "Initial Sema version"

  operating.state.sequence:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version: ChannelName (spaceheat.name), ValueList (pascal.case), TimestampList (utc.iso8601.millis)."

  pico.btu.meter.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (flattened from the scada PicoBtuMeterComponentGt runtime). ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case): the device type lived on the referenced cac, not the component. FlowMeterType: spaceheat.make.model enum value -> gw1.device.type value (pascal.case); the device-type enum is articulated by the hardware layout. ConfigList dropped."

  pico.flow.module.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component.
          - FlowMeterType: spaceheat.make.model enum value -> gw1.device.type value (pascal.case); the device-type enum is articulated by the hardware layout.
      "000":
        summary: "Initial Sema version"

  pico.tank.module.component.gt:
    latest_version: "012"
    versioning_strategy: "literal"
    versions:
      "012":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case). Context-dependent: the device type lived on the referenced cac, not the component."
      "011":
        summary: "Baseline runtime version (no prior version tracked)."
      "010":
        summary: >
          - SensorOrder: add as optional
          - PicoKOhms: required -> optional
      "000":
        summary: "Historical version back-filled from S3 wire evidence; lowest tracked version. Dual-Pico hardware identity (PicoAHwUid + PicoBHwUid) with PicoKOhms and SimpleBetaForPico temp calc; no SensorOrder."

  position.point.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  power.watts:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  price.quantity.unitless:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Rename PriceTimes1000/QuantityTimes1000 to PriceX1000/QuantityX1000."
      "000":
        summary: "Integer price/quantity pair, values x1000 (PriceTimes1000/QuantityTimes1000)."

  relay.actor.config:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: >
          - Unit: drop (redundant; unit and scaling are carried by channel identity)
          - Exponent: drop (redundant; unit and scaling are carried by channel identity)
          - CapturePeriodS / AsyncCapture / AsyncCaptureDelta / PollPeriodMs: drop (capture/report tuning moved to operational-params capture.tuning)
      "002":
        summary: >
          - StateType: add (optional on the wire for the version's first two days, 2024-12-31 to 2025-01-01)
          - DeEnergizedState: add
          - EnergizedState: add
      "001":
        summary: "Earliest wire shape, back-filled from S3 evidence. As v002 but without StateType, DeEnergizedState or EnergizedState (added 2024-12-31 with the state-consistency axioms); v001 carries only axiom 1."

  relay.control.config:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. relay.actor.config minus RelayIdx: board-resident relays are identified by name against the board (gw1.scada.device.type.gt), not by a positional index."

  report:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: "- FsmReportList[]: fsm.full.report:000 -> 001"
      "002":
        summary: >
          - StateList: add (machine.states:000)
          - FsmActionList: remove
          - FsmReportList[]: fsm.full.report:000 -> oneOf[000, 001]
      "001":
        summary: "Historical back-fill from S3 wire evidence (gwproto era). As v002 but FsmActionList (fsm.atomic.report:000) in place of StateList, and FsmReportList is fsm.full.report:000 only."

  report.event:
    latest_version: "004"
    versioning_strategy: "literal"
    versions:
      "004":
        summary: "- Axioms: reinstate ReportIdentityPropagation, ReportCreatedTimePropagation and ReportSourcePropagation"
      "003":
        summary: >
          - Report: report:002 -> 003
          - Axioms: remove former ReportIdentityPropagation and ReportCreatedTimePropagation
      "002":
        summary: "- Report: report:001 -> 002"
      "000":
        summary: "Historical back-fill from S3 wire evidence (gwproto era). As v002 but Report is report:001; same three propagation axioms."

  scada.board.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "- Initial version. ComponentId, DeviceType (gw1.device.type board kind), optional I2cAddressList (field-chosen addresses, index-aligned with the record's Expanders), DisplayName, HwUid."

  scada.control.capabilities:
    latest_version: "002"
    versioning_strategy: "literal"
    versions:
      "002":
        summary: >
          - RelayNodes[], DacNodes[], CommandNodes[]: spaceheat.node.gt:302 -> 303
          - A node may carry more than one CommandInterfaces entry, one per
            command vocabulary (five-v-boss: turn.5v.on.off and reboot.picos)
      "001":
        summary: >
          - RelayNodes[]: inline object -> spaceheat.node.gt:302
          - DacNodes[]: inline object -> spaceheat.node.gt:302
          - ControlChannels[]: inline object -> data.channel.gt:003
          - I2cRelayComponent dropped; CommandNodes[] (spaceheat.node.gt:302)
            and CommandInterfaces[] (gw.command.interface:000) added: the
            message is the cover of the command tree, one interface per node
            the root commands directly
          - Axioms: add control surface consistency checks
      "000":
        summary: "Initial Sema version"

  scada.params:
    latest_version: "005"
    versioning_strategy: "literal"
    versions:
      "005":
        summary: >
          - NewParams: ha1.params:004 | ha1.params:005 -> ha1.params:006
          - OldParams: ha1.params:004 | ha1.params:005 -> ha1.params:006
      "004":
        summary: >
          - NewParams: ha1.params:001 | ha1.params:002 | ha1.params:003 -> ha1.params:004
          - OldParams: ha1.params:001 | ha1.params:002 | ha1.params:003 -> ha1.params:004
      "002":
        summary: "- NewParams/OldParams element: ha1.params 000 -> 001/002/003"
      "001":
        summary: "Earliest params-bearing scada.params, back-filled from S3 evidence. NewParams/OldParams carry an ha1.params:000 object (nine-field shape)."
      "000":
        summary: "Earliest wire shape, back-filled from S3 evidence. A free-form param-setter envelope with no NewParams/OldParams; individual params (e.g. Gamma, BufferEmpty, DdRswt) ride additionalProperties."

  send.control.capabilities:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  send.layout:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: >
          - FromName: remove
          - ToName: remove
          - MessageCreatedMs: add
      "000":
        summary: "Initial Sema version"

  setpoint.belief:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Belief pair (Phase, optional ValueF); UnknownIsValueless axiom ties value presence to phase."

  sim.dac.writer.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (sim-marker DAC-writer component for simulated layouts; structurally i2c.dac.writer.component.gt minus board residency)."

  sim.pico.tank.module.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "- ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case); SimulatesVersion 011 -> 012. Context-dependent: the device type lived on the referenced cac, not the component. Optional SimLifeS / SimRebootS (positive.int seconds) script the simulated pico's liveness."
      "000":
        summary: "Initial Sema version"

  sim.plant.actuation:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  sim.plant.flux:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  sim.ready:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version."

  sim.relay.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (sim-marker relay component for simulated layouts)."

  sim.sensor.component.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version (new sim-marker component for simulated layouts; structurally component.gt)."

  sim.timestep:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version."

  single.machine.state:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  single.reading:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  slow.contract.rejection:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  snapshot.spaceheat:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: "LatestStateList element: machine.states -> single.machine.state (one state per entry, adds Cause; drops the parallel StateList/UnixMsList arrays). Type moved from gwproto to scada."
      "002":
        summary: >
          - LatestStateList: add (machine.states:000, parallel StateList / UnixMsList
            arrays per machine)
      "001":
        summary: "Historical back-fill from S3 wire evidence (gwproto era). As v002 but without LatestStateList (readings only). Authored as the shape carried from 2024-03-06 on; the earlier nested-Snapshot shape under the same version label predates the S3 archive."

  spaceheat.node.gt:
    latest_version: "303"
    versioning_strategy: "literal"
    versions:
      "303":
        summary: "- ActorClass: gw1.actor.class:013 -> 014 (adds FiveVBoss)"
      "302":
        summary: >
          - ActorClass: gw1.actor.class:011 -> 013 (in-place while staging)
          - InPowerMetering: drop (routing is owned by the consuming transactive declaration, not the node)
          - InPowerMeteringRequiresNameplate axiom: drop
      "301":
        summary: >
          - BoardComponentId: add as Optional
          - ActorClass: gw1.actor.class:009 -> 011
      "300":
        summary: "- ActorClass: string -> gw1.actor.class:009"
      "200":
        summary: "Baseline runtime version (no prior version tracked)."

  spaceheat.telemetry.quantity.projection:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  synced.readings:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  synced.readings.bundle:
    latest_version: "003"
    versioning_strategy: "literal"
    versions:
      "003":
        summary: >
          - Add LatePersistenceTimePeriodList: array of utc.iso8601.seconds arrays
          - Add OperatingStateSequenceList: array of operating.state.sequence:000
      "002":
        summary: "- ChannelReadingsList[]: inline object -> channel.readings.list.item:000"
      "001":
        summary: "Baseline runtime version (no prior version tracked)."

  synth.channel.gt:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  ta.deed:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  tank.module.params:
    latest_version: "200"
    versioning_strategy: "literal"
    versions:
      "200":
        summary: >
          - PicoBoardVariant: add
          - MicropythonVersion: add
      "110":
        summary: "Initial Sema version (registered from the shipped gwsproto TankModuleParams wire shape, version 110)."

  ticklist.hall:
    latest_version: "101"
    versioning_strategy: "literal"
    versions:
      "101":
        summary: "Initial Sema version"

  ticklist.hall.report:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  ticklist.reed:
    latest_version: "101"
    versioning_strategy: "literal"
    versions:
      "101":
        summary: "Initial Sema version"

  ticklist.reed.report:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  weather:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version — like-for-like port of the legacy weather_service.py wire shape onto Sema."

  weather.forecast:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial Sema version"

  web.server.component.gt:
    latest_version: "001"
    versioning_strategy: "literal"
    versions:
      "001":
        summary: "Initial Sema version (flattened from the scada WebServerComponentGt runtime; WebServer is an inline object). ComponentAttributeClassId (cac UUID) -> DeviceType (gw1.device.type value, pascal.case): the device type lived on the referenced cac, not the component. ConfigList dropped."

  zero.ten.power.on:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Initial version (NodeName, PowerOnVoltsTimesTen; TenVoltCeiling axiom). Carried per output in ZeroTenPowerOnList on both operational-params words."

  zone.circuit.governance.cmd:
    latest_version: "000"
    versioning_strategy: "literal"
    versions:
      "000":
        summary: "Governance command (FromHandle, ToHandle, Event, optional SetpointF, TriggerId, SendTimeUnixMs); SetpointIffThermostatic axiom."

enums:
  base.g.node.class:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  buffer.regulation.mode:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.heatcall.source:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.relay.pin:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.relay.state:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.valve.state:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  change.zone.call.source:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  day.of.week:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  fis.authorization.decision:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  fis.authorization.reason:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  five.v.boss.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  fsm.report.type:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  g.node.instance.status:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  g.node.instance.transport:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  g.node.status:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gpio.sense.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gpm.from.hz.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw.g.node.class:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw.house0.primary.flow.source:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw.market.product.name:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw.scada.cmd.refusal.reason:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw.weather.forecast.fidelity:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.actor.class:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "014":
        added_values:
          - "FiveVBoss"
      "013":
        added_values:
          - "I2cDacWriter"
          - "HpTwin"
      "012":
        added_values:
          - "SimSensorActor"
          - "SimRelayActor"
      "011":
        added_values:
          - "I2cThermistorReader"
      "010":
        added_values:
          - "GpioSensor"
          - "I2cBus"
          - "I2cRelayBoard"
      "009": {}

  gw1.actuation.authority:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.device.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.emission.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.heat.call.interpretation:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.lc.top.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.leaf.ally.all.tanks.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.leaf.ally.buffer.only.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.local.control.all.tanks.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.local.control.buffer.only.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.local.control.standby.top.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.main.auto.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  gw1.quantity:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "002":
        added_values:
          - "WindSpeed"
      "001":
        added_values:
          - "Time"
      "000": {}

  gw1.seasonal.storage.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.service.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.sim.device.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.system.mode:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  gw1.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "002":
        added_values:
          - "DollarsX1000"
          - "MilesPerHourX1000"
          - "KilowattHoursX1000"
      "001":
        added_values:
          - "Seconds"
          - "SecondsX10"
      "000": {}

  heatcall.source:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  hp.boss.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  hz.calc.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  i2c.adc.channel:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  i2c.adc.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.dac.channel:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.dac.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.dac.vref:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.expander.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.mux.type:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  i2c.operation:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001":
        added_values:
          - "WriteByte"
          - "ReadBytes"
      "000": {}

  log.level:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  market.price.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  market.quantity.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  market.type.name:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  pico.board.variant:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  pico.cycler.event:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  pico.cycler.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  reboot.picos:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.closed.or.open:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.energization.state:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.open.or.closed:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  relay.wiring.config:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  setpoint.phase:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  single.pico.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  spaceheat.make.model:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "008":
        added_values:
          - "GRIDWORKS__SIM_SENSOR"
          - "GRIDWORKS__SIM_RELAY_BANK"
      "007":
        added_values:
          - "SAIER__SENHZG1WA"
          - "DFROBOT__DFR0971_TIMES2"
          - "GRIDWORKS__TANKMODULE3"
          - "GRIDWORKS__GW101"
          - "GRIDWORKS__SCADA_GW108"
      "003": {}

  spaceheat.telemetry.name:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "007":
        added_values:
          - "BinaryState"
      "006": {}

  spaceheat.unit:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "001": {}

  ta.validation.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  temp.calc.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  thermistor.data.method:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  thermostat.kind:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  turn.5v.on.off:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  turn.hp.on.off:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  valve.open.or.closed:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  zone.actuator.kind:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.call.circuit.event:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.call.circuit.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.call.source:
    enum_type: "literal"
    note: "This enum is literal; version 000 is immutable and exclusive."
    versions:
      "000": {}

  zone.circuit.governance.event:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.circuit.governance.state:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.circuit.role:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}

  zone.setpoint.source:
    enum_type: "versioned"
    note: "This enum is versioned; new versions are additive only."
    versions:
      "000": {}
